


// Instantiate the module
pattern_gen instance_name (
    .rst(rst), 
    .clk(clk), 
    .Pat_in(Pat_in), 
    .PatGen_start(PatGen_start), 
    .PatGen_stop(PatGen_stop), 
    .Num_Pat(Num_Pat), 
    .CntSubc(CntSubc), 
    .FSMIND0(FSMIND0), 
    .FIFO_empty(FIFO_empty), 
    .Mask_change_subc(Mask_change_subc), 
    .FIFO_wr(FIFO_wr), 
    .Pat_out(Pat_out)
    );


